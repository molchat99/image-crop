Installation can be tricky, a common error was installation of wheels failed. In this case, install dependencies individually in a virtual environment. The python version can also be a source of error, which can be fixed by installing a different version in the env.

The script uses rembg, a model to remove background from an image. Here's the documentation and how to install: https://github.com/danielgatis/rembg
